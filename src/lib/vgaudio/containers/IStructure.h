#pragma once

namespace vgaudio {
    namespace containers {

        struct IStructure {

            virtual ~IStructure() = default;

        protected:

            IStructure() = default;

        };

    }
}
