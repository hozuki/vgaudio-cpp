#pragma once

namespace common_lib {
    namespace io {
        namespace compression {

            enum class CompressionMode {

                Decompress = 0,
                Compress = 1,

            };

        }
    }
}
